# Cosmetics Category Context

Рабочий лог по новой категории `cosmetics`.

## Текущий статус

- Категория `cosmetics` заведена как полноценная category-flow категория с отдельным blueprint:
  `views.main.categories.cosmetics.main`
- Blueprint зарегистрирован с префиксом:
  `/cosmetics`
- Используется path-based схема route-ов:
  `/cosmetics/<subcategory>/...`
- Плитка `Косметика` на странице
  `templates/main/enter_v2.html`
  ведет на `cosmetics.index`
- Для рендера вынесен helper:
  `views.main.categories.cosmetics.support.helper_cosmetics_index`
- Есть отдельная страница категории:
  `templates/categories/cosmetics/index.html`
- Для первой подкатегории используется отдельная страница оформления:
  `templates/categories/cosmetics/category_v2.html`

## Что уже сделано

- Добавлен конфиг категории в `config.py`:
  - `CATEGORY = 'косметика'`
  - `CATEGORY_PROCESS = 'cosmetics'`
  - `NUMBER_STANDART = ТР ТС 009/2011`
- Категория встроена в общие словари:
  - `CATEGORIES_DICT`
  - `SUB_CATEGORIES_DICT`
- Для косметики добавлена локальная папка `subcategories/`
- Первая подкатегория заведена как slug:
  `decor_ukhod`
- Дополнительно заведены подкатегории:
  `cosmetics_eye`
  `cosmetics_the_rest_hair`
  `razor_blades_and_cassettes`
- Добавлен registry конфигурации подкатегорий
- Добавлен каркас модели `Cosmetics` в `models.py`
- Подкатегория `decor_ukhod` оформлена как обычный category-flow:
  - шаг 1: организация и тип этикетки
  - шаг 2: параметры позиции
  - шаг 3: таблица позиций заказа

## Что уже интегрировано в обычный flow

- Добавлены route-ы:
  - `index`
  - `copy_order`
  - `preprocess_order`
  - `delete_order_pos`
  - `clean_orders`
  - `process_order`
- Категория встроена в общие helper’ы и обработчики:
  - `preprocess_order_category`
  - `orders_list_common`
  - `order_count`
  - `check_order_pos`
  - `helper_delete_order_pos`
  - `helper_process_category_order`
  - `order_table_update`
- Категория встроена в общую логику:
  - сохранения позиции в БД
  - копирования позиции
  - удаления позиции
  - пагинации таблицы
  - расчета количества и суммы
  - валидации ТН ВЭД

## Что сделано по UI/JS

- Шаг 2 и шаг 3 вынесены в:
  - `templates/helpers/cosmetics/decor_ukhod/2nd_step.html`
  - `templates/helpers/cosmetics/decor_ukhod/3rd_step.html`
- Общие блоки косметики вынесены в:
  - `templates/helpers/cosmetics/countries_rd_switch.html`
  - `templates/helpers/cosmetics/rd_block.html`
  - `templates/helpers/cosmetics/response_cosmetics_table.html`
  - `templates/helpers/cosmetics/table_actions.html`
- Добавлен отдельный JS категории:
  `static/main_v2/js/categories/cosmetics.js`
- На стартовой странице категории
  `templates/categories/cosmetics/index.html`
  добавлен быстрый поиск подкатегории:
  - поиск работает локально, без AJAX
  - ищет только по номеру ТН ВЭД и по виду товара
  - поиск начинается с 3-го символа
  - если совпадений несколько, показывается список
  - в выдаче показывается не больше 15 результатов
  - если результатов больше 15, снизу выводится сообщение
    `Результатов поиска больше 15 ...`
  - весь найденный пункт кликабелен целиком
  - блок выдачи оформлен как белая карточка с желтыми разделителями
  - `razor_blades_and_cassettes` тоже участвует в поиске:
    по `ТН ВЭД`
    по `Виду товара`
- На плитке `Косметика` на странице
  `templates/main/enter_v2.html`
  добавлен бейдж `New`
- В левом боковом меню под `Обычный заказ`
  добавлена отдельная ссылка `Косметика`
  на общую страницу `cosmetics.index`
  с бейджем `New`
- Подключена superuser-кнопка тестового заполнения
- В `ordinary_order_test_fill.js` добавлен сценарий тестового заполнения для `cosmetics`
- Для `decor_ukhod` согласованы текущие дефолты и ограничения:
  - код категории `30227`
  - выбор ТН ВЭД через модалку по клику в поле
  - разрешён только 1 код:
    `3304990000`
  - на странице заполнения этот ТН ВЭД теперь предзаполняется автоматически
  - `Номинальное количество = 1`
  - `Тип ном. кол-ва = шт`
  - `Срок годности, мес. = 36`
  - `Состав товара maxlength = 2000`
  - `Дата от` не раньше `01.01.2023`
  - `Дата до` не раньше чем `сегодня + 1 месяц`
- Для `cosmetics_lips` заведён отдельный конфиг по той же схеме, что `decor_ukhod`:
  - slug: `cosmetics_lips`
  - заголовок: `Косметика для губ`
  - код категории:
    `31051`
  - разрешённые ТН ВЭД:
    `3304990000`, `3304100000`
  - все остальные справочники и ограничения переиспользуются из `decor_ukhod`
  - подключена отдельная иконка:
    `main_v2/img/icons/cosmetics_lips.svg`
  - сейчас просто закомментирована в enum/registry, поэтому скрыта из рабочего выбора
  - на стартовую страницу `cosmetics` добавлена отдельная disabled-плитка `Косметика для губ` с плашкой `Скоро`
- Для `cosmetics_eye` заведён базовый конфиг подкатегории:
  - slug: `cosmetics_eye`
  - заголовок: `Косметика для глаз`
  - под неё добавлен Excel-справочник:
    `views/main/categories/cosmetics/Косметика_для_глаз.xlsx`
  - код категории обновлён по справочнику:
    `31049`
  - разрешённые ТН ВЭД сейчас:
    `3304990000`, `3304200000`
  - список `Вид товара` синхронизирован по справочнику из Excel
  - сейчас просто закомментирована в enum/registry, поэтому скрыта из выбора на стартовой странице
  - на стартовую страницу `cosmetics` добавлена отдельная disabled-плитка `Косметика для глаз` с плашкой `Скоро`
  - для стартовой страницы и будущего включения используется иконка:
    `main_v2/img/icons/cosmetics_eye.svg`
  - для CRM добавлена отдельная иконка:
    `crm_mod/img/icons/cosmetics_eye.svg`
  - это временное UI-решение до включения подкатегории в рабочий flow
  - после полноценного включения `cosmetics_eye` нужно удалить временные disabled-стили и отдельный блок `coming soon` из `templates/categories/cosmetics/index.html`
- Для `cosmetics_lips` после полноценного включения тоже нужно удалить временный disabled-блок из `templates/categories/cosmetics/index.html`
- Для `cosmetics_the_rest_hair` заведён конфиг по Excel-справочнику
  `views/main/categories/cosmetics/Прочие_косметические_средства_для_волос.xlsx`:
  - slug: `cosmetics_the_rest_hair`
  - заголовок: `Прочие косметические средства для волос`
  - код категории:
    `234573`
  - разрешённый ТН ВЭД:
    `3305900009`
  - на странице заполнения этот ТН ВЭД теперь предзаполняется автоматически
  - добавлен список видов товара из справочника
  - добавлены стандартные типы номинального количества:
    `мл`, `л`, `г`, `кг`, `шт`
- Для `razor_blades_and_cassettes` заведена рабочая подкатегория
  `Бритвы и лезвия`:
  - slug:
    `razor_blades_and_cassettes`
  - используется отдельный шаблон шага 2:
    `templates/helpers/cosmetics/razor_blades_and_cassettes/2nd_step.html`
  - шаг 3 использует общий cosmetics-table, но с урезанным набором колонок
  - в форме добавлены отдельные поля:
    `Кол-во в потреб. упаковке`
    `Тип ед. в потреб. упаковке`
    `Кол-во лезвий в раб. части`
    `Комплектация`
  - `Кол-во лезвий в рабочей части` по умолчанию = `1`
  - `Комплектация` хранится в БД в `Cosmetics.complectation`
  - `Кол-во лезвий` хранится в БД в `Cosmetics.blade_count`
  - для `complectation` добавлен `server_default=''`
  - в UI используется единый широкий формат таблицы с колонкой `Комплектация`
  - для `одноразовых` и `лезвий` `Комплектация` остается пустой
  - список разрешённых ТН ВЭД:
    `8212101000`
    `8212109000`
    `8212200000`
  - логика выбора ТН ВЭД завязана на `Вид товара`
  - если по виду товара возможны и одноразовый, и многоразовый сценарии, пользователь выбирает ТН ВЭД вручную
  - `Комплектация` показывается только для многоразового сценария через `ТН ВЭД = 8212109000`
  - серверная валидация использует те же маппинги, что и client-side логика
  - подкатегория участвует в поиске на стартовой странице `cosmetics`
  - для CRM добавлена отдельная иконка:
    `crm_mod/img/icons/cosmetics_razor.svg`
- Страны приведены к uppercase
- В шаг 2 добавлено автогенерируемое `Полное наименование`:
  - собирается из `Вид товара + Товарный знак + Дополнение`
  - показывается как серый display-блок, а не как editable input
  - есть отдельный switch `Доп.` и поле `Дополнить полное наименование`
  - если товарный знак = `БЕЗ ТОВАРНОГО ЗНАКА`, он не попадает в онлайн-display полного наименования
  - если при `БЕЗ ТОВАРНОГО ЗНАКА` итоговое полное наименование состоит только из `Вида товара`, такую позицию сохранять нельзя
  - в этом случае пользователь должен либо вернуть реальный `Товарный знак`, либо заполнить `Дополнить полное наименование`
  - правило проверяется и на UI до отправки формы, и на сервере в `ValidatorProcessor.validate_cosmetics_subcategory_payload(...)`
  - на UI ошибка попадает в общий список ошибок формы, а поля `Товарный знак` и `Дополнить полное наименование` подсвечиваются как invalid
- Заголовочная часть страницы `cosmetics` приведена к общему паттерну остальных категорий:
  - `Новая накладная` показывается только без `o_id`
  - при открытой накладной остаётся `Накладная`
  - текст `Создать заказ` скрывается, если заказ уже открыт
- Для `Состав товара` добавлена клиентская нормализация:
  - допускается кириллица, цифры, знаки препинания и служебные символы из whitelist
  - латинская раскладка автоматически переводится в русскую
  - автоперекладка работает только для букв и не должна ломать ввод `, . : ;`
  - рядом с label добавлен tooltip с этим правилом
- Поле `Для детей` в форме косметики укорочено:
  - label изменён с `Предназначено для детей` на `Для детей`
  - рядом добавлена красная tooltip-иконка
  - tooltip: `Выберите, предназначен товар для детей или нет.`
- Поле `Товарный знак` приведено к паттерну других категорий:
  - `check_article_word(...)`
  - `check_valid(...)`
  - `normalizeTrademarkPlaceholder(...)`
  - `noTMSwitch`
- РД-блок приведен к логике обычного заказа:
  - в обычной форме только `Тип`, `Код/Название`, `Дата выдачи`
  - при переключении `РД` меняется список стран
  - при переключении `РД` сбрасывается выбранная страна
  - при выключении `РД` очищаются поля блока
- Для `cosmetics` включен лоадер на `Добавить/Изменить` по паттерну других категорий
- Для `cosmetics` подключена склейка одинаковых позиций:
  - при обычном добавлении позиции
  - при копировании заказа из истории
  - для merge-key у `Cosmetics` количество позиции (`quantity`) не участвует в сравнении, а только суммируется
  - для `razor_blades_and_cassettes` в merge-key дополнительно включены:
    `blade_count`
    `complectation`
  - `tnved_code` уже участвует в общем merge-key и не дает склеивать одноразовые и многоразовые позиции между собой
- В истории заказов для `cosmetics` добавлено отображение подкатегории в строке заказа:
  - при фильтре `Все категории`
  - при фильтре `Косметика`
- Добавлена кастомная client-side проверка периода годности:
  - если `Дата до` позже чем `Дата от + срок годности в месяцах`, позиция не добавляется
  - ошибка попадает в общий список ошибок формы
  - поле `Дата до` подсвечивается как invalid
- Для вида товара `МАСКА` добавлен отдельный спец-список `Тип ном. кол-ва`:
  - разрешены только `г` и `мл`
  - при выборе `МАСКА` список и дефолт `Тип ном. кол-ва` переключаются автоматически
  - при возврате на любой другой вид товара возвращается общий список и обычный дефолт категории

## Что уже фиксили по багам

- Исправлено распознавание `settings.Cosmetics.CATEGORY` в `preprocess_order_common`
- Исправлены redirect/build URL для `clean_orders` с учетом `subcategory`
- Исправлено AJAX-обновление таблицы через `order_table_update` с учетом `subcategory`
- Исправлены `url_for(...)` для subcategory-based route-ов косметики
- Исправлены action-кнопки таблицы шага 3 под тот же паттерн, что в других категориях
- Исправлена правая плашка шага 2:
  добавлен нормальный отступ между ключом и значением
- Из правой плашки шага 2 убрана строка `Сумма по накладной / Не указано` для общих cosmetics-шаблонов `decor_ukhod` и `razor_blades_and_cassettes`; JS-обработчиков суммы для cosmetics нет.
- Исправлено закрытие лоадера после успешного AJAX-добавления позиции
- Исправлен `onclick` у кнопки `Смотреть` в шаге 3
- Исправлен парсинг дат РД/дат периода годности под фактический формат формы
- Исправлены `change_org` ссылки/редиректы для `cosmetics` с обязательной `subcategory`
- Исправлено отображение плитки `Косметика` в архиве под размер/стиль остальных категорий
- Исправлено переключение спец-списка `Тип ном. кол-ва` для масок через фактический `select2/change` flow
- Исправлен блок ТН ВЭД для `decor_ukhod`:
  - вместо одного фиксированного кода добавлен выбор из 4 разрешённых кодов
  - выбор открывается по клику в поле, по тому же modal/accordion паттерну, что и в других категориях
  - ручной ввод по-прежнему не используется, поле остаётся `readonly`
- Исправлен предпросмотр заказа для `cosmetics`:
  - на странице категории добавлен блок `Просмотр заказа / Смотреть`
  - ссылка на `preload` теперь строится с обязательной `subcategory`
- Исправлена логика полного наименования и РД в export/preload:
  - `БЕЗ ТОВАРНОГО ЗНАКА` не должен попадать в полное наименование
  - строка разрешительной документации формируется по общему паттерну остальных категорий
- Исправлена нормализация поля `Состав товара`:
  - разрешён ввод знаков и символов из whitelist
  - знаки `, . : ;` больше не превращаются в русские буквы из-за автоперекладки
- Исправлен стартовый экран `cosmetics`:
  - добавлен быстрый поиск подкатегории по ТН ВЭД / виду товара
  - поиск больше не использует описание ТН ВЭД как источник совпадений
  - при нескольких совпадениях выводится список, а не только первый результат
  - выдача ограничена первыми 15 совпадениями
  - при числе совпадений больше 15 показывается отдельное сообщение
  - весь блок результата сделан кликабельным, с hover-подсветкой
- Исправлены точки входа в `cosmetics` в основном UI:
  - у `Косметика` в `enter_v2` бейдж переименован из `Новинка` в `New`
  - в боковом меню добавлена ссылка `Косметика` под `Обычный заказ`
  - у `ИИ съемка` убран бейдж `Новинка`, чтобы не конкурировал с запуском `cosmetics`
- Для `razor_blades_and_cassettes` отдельно исправлены:
  - показ `Комплектации` только для подходящего сценария
  - фильтрация ТН ВЭД по `Виду товара`
  - автоподстановка однозначного ТН ВЭД по `Виду товара`
  - скрытие лишних полей `Тип состава` / `Состав` / `Для детей` в просмотре позиции на шаге 3
  - отдельный export/preload-формат без старых косметических полей
  - определение `Кода категории` по `ТН ВЭД`, а не по одному общему коду подкатегории

## Что подтянуто из main

- Ветка `features/cosmetics` влита с актуальным `origin/main`
- Merge commit:
  `f9194e79`
- При merge вручную разведены конфликты в:
  - `app/utilities/helpers/h_categories.py`
  - `app/utilities/support.py`
- Из `main` подтянута общая логика:
  - loader-flow для category JS
  - унификация/склейка одинаковых позиций через `append_or_merge_position(...)`
  - сопутствующие изменения общих helper’ов

## Preload / Export / Archive

- Для `cosmetics` добавлен отдельный `preload` route
- Добавлен `CosmeticsProcessor` по паттерну `parfum`, но со своей таблицей
- Добавлены отдельные шапки:
  - `COSMETICS_START_EXT`
  - `COSMETICS_PRELOAD_START`
- Для `razor_blades_and_cassettes` добавлены отдельные шапки:
  - `COSMETICS_RAZOR_START_EXT`
  - `COSMETICS_RAZOR_PRELOAD_START`
- В `preload` колонка названа:
  - `Декларация соответствия`
- Для косметики в preload/export выведены:
  - `Код категории`
  - `Полное наименование`
  - `Дата_от`
  - `Дата_до`
  - `Количество`
  - `Страна`
  - `Декларация соответствия`
- `Полное наименование` в preload/export генерируется из:
  - `Вид товара + Товарный знак + Дополнение`
- В export-таблицу `cosmetics` добавлена первая колонка:
  - `Код товара`
  - в подстроках шаблона это `GTIN` / `value`
  - колонка пока не обрабатывается и уходит пустой
  - в `preload` эта колонка не выводится
- Поле `Для детей` в preload/export нормализуется жёстко:
  - `ДА` только если значение поля равно `ДА`
  - любое другое значение, включая пустое, уходит в `НЕТ`
- Для `Cosmetics` добавлено поле `full_name_extra`
  - модель обновлена
  - сохранение/копирование/merge-key обновлены
  - добавлена миграция `0d3b3dbfce12_add_full_name_extra_to_cosmetics.py`
- Для `Cosmetics` добавлены поля:
  - `blade_count`
  - `complectation`
  - добавлена миграция:
    `6b5f7f8f4b9d_add_cosmetics_razor_fields.py`
- Для `razor_blades_and_cassettes` экспорт и preload работают по отдельному широкому профилю:
  - колонка `Комплектация` присутствует всегда
  - `Код категории` определяется по `ТН ВЭД`
  - для `8212101000`:
    `30958`
  - для `8212109000`:
    `30957`
  - для `8212200000`:
    `231207`
- В истории заказов добавлена плитка `Косметика`
- Для черновиков `cosmetics` используется логика по `subcategory`:
  - в общем `order_book_detail` косметика показывается по подкатегориям, как одежда
  - ссылки открытия черновика и очистки черновика строятся с обязательной `subcategory`
- Лимит активных черновиков для `cosmetics` действует по каждой подкатегории отдельно:
  - не более 5 заказов на одну подкатегорию
- Для CRM обычных заказов добавлена базовая поддержка `косметика`:
  - отдельная плитка категории в фильтрах CRM
  - отдельная иконка категории в карточке CRM-заказа
  - внутренняя иконка `cosmetics` в круге карточки заказа уменьшена, чтобы был больший отступ от краёв бейджа
  - фильтр по плитке работает через общий category-flow CRM
  - подсчёт `КС/КМ` включён в общие SQL-агрегации CRM
  - при переключении плиток категорий показывается общий CRM-loader
- Для подкатегорий `cosmetics_eye` и `cosmetics_the_rest_hair` добавлены отдельные иконки:
  - на странице выбора подкатегорий `cosmetics`
  - в CRM-карточках и списках заказов
  - для `cosmetics_the_rest_hair` основная и CRM-иконка сейчас синхронизированы один в один по рисунку
- Для `cosmetics_mochalki` заведена рабочая подкатегория по Excel-справочнику
  `views/main/categories/cosmetics/Мочалки_и_губки.xlsx`:
  - slug: `cosmetics_mochalki`
  - заголовок: `Мочалки и губки`
  - код категории:
    `30644`
  - разрешённые ТН ВЭД:
    `7323100000`
    `7418109000`
    `7615108001`
    `3924900001`
  - `Номинальное количество` ограничено типом:
    `шт`
  - список `Вид товара` синхронизирован по справочнику из Excel
  - подкатегория включена в `enum/registry`
  - плитка на стартовой странице `cosmetics` переведена из `Скоро` в рабочую
  - используется иконка:
    `main_v2/img/icons/cosmetics_mochalki.svg`
  - для CRM заведена отдельная иконка:
    `crm_mod/img/icons/cosmetics_mochalki.svg`
- Для `cosmetics_rascheski` заведена рабочая подкатегория по Excel-справочнику
  `views/main/categories/cosmetics/Расчёски,_щётки_и_гребни_для_волос.xlsx`:
  - slug: `cosmetics_rascheski`
  - заголовок: `Расчёски, щётки и гребни для волос`
  - код категории:
    `30596`
  - разрешённые ТН ВЭД:
    `9603293000`
    `9615110000`
    `9615190000`
  - `Номинальное количество` ограничено типом:
    `шт`
  - список `Вид товара` синхронизирован по справочнику из Excel
  - подкатегория включена в `enum/registry`
  - плитка на стартовой странице `cosmetics` переведена из `Скоро` в рабочую
  - используется иконка:
    `main_v2/img/icons/cosmetics_rascheski.svg`
  - для CRM заведена отдельная иконка:
    `crm_mod/img/icons/cosmetics_rascheski.svg`
- Для `razor_blades_and_cassettes` тоже добавлена отдельная иконка в CRM:
  - используется в CRM-карточках
  - используется в CRM-списках заказов
  - файл:
    `crm_mod/img/icons/cosmetics_razor.svg`
- Иконки косметики в CRM приведены к единому порядку:
  - все подкатегории `cosmetics` в CRM используют отдельные файлы из
    `crm_mod/img/icons/`
  - для `decor_ukhod` заведён отдельный CRM-файл:
    `crm_mod/img/icons/cosmetics_decor_ukhod.svg`
  - для `cosmetics_lips` заведён отдельный CRM-файл:
    `crm_mod/img/icons/cosmetics_lips.svg`
  - для `cosmetics_salt_bomb` заведён отдельный CRM-файл:
    `crm_mod/img/icons/cosmetics_salt_bomb.svg`
  - для `cosmetics_mochalki` заведён отдельный CRM-файл:
    `crm_mod/img/icons/cosmetics_mochalki.svg`
  - для `cosmetics_rascheski` заведён отдельный CRM-файл:
    `crm_mod/img/icons/cosmetics_rascheski.svg`
  - CRM-шаблоны больше не должны смешивать иконки `main_v2` и `crm_mod`
    для subcategory-иконок косметики

## Что сейчас остаётся проверить/добить

- Проверить руками весь сценарий:
  - первая позиция -> redirect
  - вторая и следующие -> AJAX
  - повторное добавление идентичной позиции -> склейка
  - копирование заказа из истории -> склейка идентичных позиций
  - copy/edit/delete
  - clean order
  - process order
- Отдельно проверить руками `razor_blades_and_cassettes`:
  - автоподстановку / выбор ТН ВЭД по `Виду товара`
  - исчезновение `Комплектации` при смене на неподходящий `Вид товара` или `ТН ВЭД`
  - `Код категории` в preload/export для всех трех сценариев:
    одноразовые
    многоразовые
    лезвия / кассеты
  - отсутствие ошибочного склеивания позиций при разной `Комплектации` или разном `Кол-ве лезвий`
- Проверить руками спец-логику для масок:
  - переключение списка `Тип ном. кол-ва`
  - дефолт при выборе маски
  - возврат общего списка при смене вида товара
- Проверить руками поведение `Состав товара`:
  - конвертацию латинской раскладки в русскую
  - работу tooltip
- Проверить руками CRM по `косметика`:
  - фильтр по плитке категории
  - отображение иконки категории в карточке заказа
  - корректность подсчёта `КС/КМ`
  - loader при переключении категорий
- Проверить, нет ли ещё скрытых `BuildError` в общих шаблонах/модалках, где косметике нужен `subcategory`
- Проверить, создана ли физически таблица `cosmetics` в БД
- Доделать серверные бизнес-проверки по датам и связанным полям, если потребуется
- Добавить остальные подкатегории косметики
- Уточнить и при необходимости пересобрать справочник для `cosmetics_eye`
- При необходимости отдельно дожать визуальную унификацию SVG только через параметры контейнера, без перерисовки самих значков

## Важные замечания

- Локальная ветка `features/cosmetics` уже подтянута к актуальному `origin/main`
- `README` актуализирован под текущее состояние этой ветки перед пушем

## Актуализация 2026-06-17

- Для `cosmetics_salt_bomb` подкатегория включена в рабочий flow:
  - добавлена в `subcategories/registry.py`
  - на стартовой странице `cosmetics` больше не рендерится как disabled/`Скоро`
  - иконка приведена к серому тону остальных иконок
  - для `3307300000` описание приведено к формулировке:
    `Ароматизированные соли и прочие составы для принятия ванн`
- Для формы `decor_ukhod` подпись
  `Хар-ка срока использования`
  укорочена до
  `Хар-ка срока исп-ния`
- Для `razor_blades_and_cassettes` обновлены подписи блока срока годности:
  - `Срок службы мес.` -> `Срок годности, мес.`
  - `Дата от` -> `Период годности: дата от`
  - `Дата до` -> `Период годности: дата до`
- В модалке `Смотреть` для `cosmetics` поле `Количество лезвий` теперь показывается только для подкатегории
  `Бритвы и лезвия`
- Подключение `static/main_v2/js/categories/cosmetics.js` переведено на `?v=7`
- Для открытия заказа `cosmetics` включена фильтрация позиций по `subcategory`:
  - внутри заказа больше не должны смешиваться позиции разных подкатегорий косметики
  - `copy_order` для `cosmetics` тоже ограничен той же `subcategory`
- Лимит активных черновиков для `cosmetics` действует по каждой подкатегории отдельно:
  - не более 5 заказов на одну подкатегорию
- В общей модалке `Черновик заказов` косметика теперь показывается по подкатегориям, как одежда:
  - группировка идёт по `subcategory`
  - подписи берутся из `settings.SUB_CATEGORIES_DICT`
  - для unknown/empty slug в шаблоне оставлен safe fallback через `.get(...)`

## Актуализация 2026-06-19

- Для `cosmetics_tooth` включён рабочий flow подкатегории
  `Средства гигиены полости рта`
- Для подкатегории подключена пользовательская иконка:
  `main_v2/img/icons/cosmetics_tooth.svg`
- Для CRM подключена иконка:
  `crm_mod/img/icons/cosmetics_tooth.svg`
- Зубная иконка переведена в серый контурный стиль, визуально совместимый с остальными иконками подкатегорий
- В одну подкатегорию сведены 4 справочника:
  - `Зубные_пасты,_порошки.xlsx`
  - `Другие_средства_для_ухода_за_полостью_рта.xlsx`
  - `Нити_для_чистки_зубов.xlsx`
  - `Зубные_щётки_механические.xlsx`
- Внутри `cosmetics_tooth` есть 4 разные товарные группы, и различаются они не только названием:
  - `Зубные пасты, порошки`
    ТН ВЭД `3306100000`, код категории `30619`, типы ном. кол-ва: `мл`, `л`, `г`, `кг`, `шт`
  - `Прочие средства для ухода за полостью рта`
    ТН ВЭД `3306900000`, код категории `30620`, типы ном. кол-ва: `мл`, `л`, `г`, `кг`, `шт`
  - `Нити для чистки зубов`
    ТН ВЭД `3306200000`, код категории `31044`, типы ном. кол-ва: `шт`, `мм`, `см`, `м`
  - `Зубные щётки механические`
    ТН ВЭД `9603210000`, код категории `30618`, типы ном. кол-ва: только `шт`
- Для `cosmetics_tooth` логика выбора данных привязана к `Виду товара`:
  - через `tnved_codes_by_product_type` ограничивается допустимый ТН ВЭД
  - через `category_code_by_tnved` вычисляется код категории
  - через `nominal_quantity_types_by_product_type` на клиенте ресетится список `Тип ном. кол-ва`
- Для oral-подкатегории учтён смешанный сценарий по полю состава:
  - для `3306100000` и `3306900000` показывается `Тип состава`
  - для `3306200000` и `9603210000` блок `Тип состава` скрывается
  - label поля состава переключается между
    `Состав товара`
    и
    `Состав товара / материал изделия`
- На пользовательской стороне при смене `Вида товара` сразу пересчитываются зависимые поля:
  - допустимый ТН ВЭД
  - код категории
  - список `Тип ном. кол-ва`
  - label поля состава
  - показ/скрытие `Типа состава`
- В `Предпросмотре` для `cosmetics_tooth` выведены 3 отдельные колонки:
  - `Тип состава`
  - `Состав товара`
  - `Состав товара / материал изделия`
- В скачиваемом Excel для `cosmetics_tooth` используется расширенный шаблон:
  - `Состав товара` занимает 2 физические колонки: `type` и `value`
  - `Состав товара / материал изделия` занимает отдельную колонку `value`
  - заполнение идёт по ТН ВЭД:
    для паст/прочих средств `content_type -> Тип состава`, `content -> Состав товара`
    для нитей/щёток `content -> Состав товара / материал изделия`
- `utilities/download.py` нормализует поле состава товара только на финальной Excel-выгрузке:
  переносы строк убираются; если перед переносом нет запятой, ставится `, `, если запятая уже есть, ставится пробел.
  Это защищает 1C-обработчик от `_x000D_` в XLSX.

## Актуализация 2026-06-22

- На стартовую страницу `cosmetics` добавлены ещё 2 disabled-плитки с плашкой `Скоро`:
  - `Товары для ароматизации`
    `main_v2/img/icons/cosmetics_aroma.svg`
  - `Средства моющие, стиральные, чистящие`
    `main_v2/img/icons/cosmetics_cleaning_products.svg`
- Для `cosmetics_aroma.svg` и `cosmetics_cleaning_products.svg` сохранён исходный рисунок:
  - иконки переведены в серый тон
  - контур утолщён под стиль соседних иконок
  - форма самих иконок не менялась
- На стартовой странице `cosmetics` обновлён query-version у `img src` в:
  `templates/categories/cosmetics/index.html`
  - это сделано для принудительного обновления SVG из браузерного кэша
- Порядок плиток подкатегорий на странице выбора задаётся вручную в:
  `views/main/categories/cosmetics/support.py`
  - список slug внутри `cosmetics_category_tiles` является эталонным порядком
  - этот порядок нужно сохранять без перестановок
  - все следующие новые подкатегории нужно добавлять ниже уже существующих, в конец списка
- Зафиксирован текущий ручной порядок плиток на стартовой странице `cosmetics`:
  - `decor_ukhod`
  - `cosmetics_eye`
  - `cosmetics_lips`
  - `cosmetics_the_rest_hair`
  - `cosmetics_rascheski`
  - `razor_blades_and_cassettes`
  - `cosmetics_tooth`
  - `cosmetics_salt_bomb`
  - `cosmetics_mochalki`
  - `cosmetics_aroma`
  - `cosmetics_cleaning_products`

## Актуализация 2026-06-23

- Подкатегория `cosmetics_aroma` переведена из teaser-состояния в рабочую:
  - заголовок: `Товары для ароматизации`
  - Excel-справочник:
    `views/main/categories/cosmetics/Товары_для_ароматизации.xlsx`
  - код категории:
    `30624`
  - разрешённый ТН ВЭД:
    `3307490000`
  - список `Вид товара` синхронизирован по Excel-справочнику
  - используется иконка:
    `main_v2/img/icons/cosmetics_aroma.svg`
  - подключена в `CosmeticsSubcategories`, `registry` и поиск стартовой страницы
- Подкатегория `cosmetics_cleaning_products` переведена из teaser-состояния в рабочую:
  - заголовок: `Средства моющие, стиральные, чистящие`
  - Excel-справочник:
    `views/main/categories/cosmetics/Средства_моющие,_стиральные,_чистящие.xlsx`
  - код категории:
    `30175`
  - разрешённые ТН ВЭД:
    `3402500000`
    `3405400000`
  - список `Вид товара` синхронизирован по Excel-справочнику
  - используется иконка:
    `main_v2/img/icons/cosmetics_cleaning_products.svg`
  - подключена в `CosmeticsSubcategories`, `registry` и поиск стартовой страницы
- Для обеих новых подкатегорий переиспользован базовый flow `decor_ukhod`:
  - шаг 2:
    `helpers/cosmetics/decor_ukhod/2nd_step.html`
  - шаг 3:
    `helpers/cosmetics/decor_ukhod/3rd_step.html`
  - справочники `Для детей`, `Срок использования`, `Тип состава`, страны и базовые `Типы ном. кол-ва` работают по общей схеме косметики
- Disabled-плашки `Скоро` для `cosmetics_aroma` и `cosmetics_cleaning_products` убраны со стартовой страницы:
  - обе плитки теперь ведут в рабочие формы
- Человекочитаемые названия подкатегорий добавлены в:
  `utilities/param_lists.py -> SUB_CATEGORIES_DICT`

- Обновлена страница распределения `cosmetics`:
  - у `cosmetics_aroma` подтянулась новая иконка
  - добавлены ещё 2 disabled-плитки с плашкой `Скоро`:
    `Парфюмерия`
    `Средства для ногтей`
  - для них используются иконки:
    `main_v2/img/icons/cosmetics_perfume.svg`
    `main_v2/img/icons/cosmetics_nails.svg`
  - новые teaser-плитки добавлены в конец ручного порядка, не ломая текущую последовательность
  - обновлён query-version у `img src` в:
    `templates/categories/cosmetics/index.html`
    чтобы браузер подтянул новые SVG
- Иконки `cosmetics_aroma.svg` и `cosmetics_nails.svg` приведены к общему косметическому стилю:
  - серый цвет:
    `#737373`
  - базовая толщина контура для новых teaser-иконок:
    `stroke-width="2"`
  - скругление соединений:
    `stroke-linejoin="round"`

## Актуализация 2026-06-23 CRM

- Для `cosmetics_aroma` добавлена CRM-иконка в заказы:
  - файл:
    `static/crm_mod/img/icons/cosmetics_aroma.svg`
  - источник синхронизирован из:
    `static/main_v2/img/icons/cosmetics_aroma.svg`
  - иконка подключена в обеих CRM-шаблонах заказов:
    `templates/product_cards/crm/helpers/order_category.html`
    `templates/crm_mod_v1/helpers/order_category.html`
- Для `cosmetics_aroma.svg` отдельно усилен визуальный вес самой иконки:
  - `stroke-width` увеличен до `3`
  - добавлен `stroke-linecap="round"`

## Актуализация 2026-06-25

- Для подкатегорий
  `cosmetics_mochalki`
  и
  `cosmetics_rascheski`
  обработка поля состава приведена к справочным Excel:
  - label в пользовательской форме:
    `Состав товара / материал изделия`
  - тот же label используется в модалке `Смотреть`
  - блок `Тип состава` для этих двух подкатегорий скрыт
- Тот же паттерн протянут в `preload` и в скачиваемые таблицы:
  - для таких подкатегорий не используется колонка `Тип состава`
  - в `preload` показывается колонка
    `Состав товара / материал изделия`
  - в Excel-выгрузке используется явный structural-config по полям подкатегории
- Поведение сделано через subcategory-config:
  - остальные подкатегории `cosmetics` не затронуты
- Для следующих подкатегорий с тем же паттерном
  `материал изделия` / без `Типа состава`
  нужно использовать тот же config-подход:
  - в subcategory-модуле:
    `CONTENT_TYPE_ENABLED = False`
  - в subcategory-модуле:
    `DEFAULT_CONTENT_LABEL = "Состав товара / материал изделия"`
  - затем прокинуть явные export-флаги в `subcategories/registry.py`
  - базовый паттерн для material-only подкатегорий:
    `export_has_material_field = True`
    `export_has_children_field = True`
  - дополнительный JS или отдельный `download.py`-if под конкретный slug больше не нужен
- Поднят cache-bust для пользовательского JS категории:
  - `templates/categories/cosmetics/category_v2.html`
    `cosmetics.js?v=12`
  - `templates/categories/cosmetics/index.html`
    `cosmetics.js?v=12`

## Актуализация 2026-06-28

- В рабочий flow включены две новые подкатегории:
  - `cosmetics_toilet_paper`
  - `cosmetics_tweezers`
- Обе подкатегории убраны из teaser/disabled состояния на стартовой странице `cosmetics` и переведены в обычный рабочий выбор.
- Для `cosmetics_toilet_paper` заведён отдельный конфиг по справочнику `Бумага_туалетная.xlsx`:
  - код категории:
    `30647`
  - разрешённые ТН ВЭД:
    `4818101000`
    `4818109000`
  - добавлено отдельное поле шага 2:
    `Кол-во слоев`
  - варианты поля:
    `ОДНОСЛОЙНОЕ`
    `ДВУХСЛОЙНОЕ`
    `ТРЁХСЛОЙНОЕ`
    `ЧЕТЫРЁХСЛОЙНОЕ`
    `ПЯТИСЛОЙНОЕ`
    `МНОГОСЛОЙНОЕ`
  - поле сохраняется в БД в:
    `Cosmetics.layers_characteristic`
  - для этого поля добавлена миграция:
    `4f8de58f6a21_add_layers_characteristic_to_cosmetics.py`
  - в таблице шага 3 отдельная колонка по слоям не показывается
  - в модалке `Смотреть` слой показывается как:
    `Кол-во слоев`
- Для `cosmetics_tweezers` заведён отдельный конфиг по справочнику `Пинцеты.xlsx`:
  - код категории:
    `30956`
  - разрешённый ТН ВЭД:
    `8203200001`
- По договорённости от 2026-06-28:
  - у `cosmetics_toilet_paper` и `cosmetics_tweezers` обязательно остаются поля:
    `Состав товара / материал изделия`
    `Срок годности, мес.`
    `Период годности: дата от`
    `Период годности: дата до`
  - у `cosmetics_toilet_paper` остаются:
    `Кол-во слоев`
    `Для детей`
    `Состав товара / материал изделия`
  - у `cosmetics_tweezers` скрыто поле:
    `Для детей`
    но поле
    `Состав товара / материал изделия`
    остаётся в форме и в выгрузках
- Поведение новых подкатегорий вынесено в subcategory-config:
  - `content_type_enabled`
  - `content_value_enabled`
  - `for_children_enabled`
  - `layers_characteristic_choices`
  - `export_has_material_field`
  - `export_has_children_field`
  - `export_has_layers_field`
- Под новые формы/модалку поднят cache-bust пользовательского JS:
  - `templates/categories/cosmetics/category_v2.html`
    `cosmetics.js?v=12`

## Актуализация 2026-06-29

- Исправлено тестовое автозаполнение обычного заказа для `cosmetics`:
  - сценарий в `static/main_v2/js/ordinary_order_test_fill.js`
    теперь учитывает `subcategory`
  - для `cosmetics_toilet_paper` автозаполнение теперь выбирает
    `Кол-во слоев = ДВУХСЛОЙНОЕ`
  - это закрывает client-side required-поле `layers_characteristic`
    и позволяет корректно добавлять тестовую позицию через superuser-кнопку
- Для `cosmetics_toilet_paper` и `cosmetics_tweezers` доведено отображение иконок в CRM:
  - обычный CRM-список заказов
  - CRM helper-таблицы
  - CRM product cards
  - для новых slug в `cosmetics_icon_map` используются рабочие пользовательские svg:
    `main_v2/img/icons/cosmetics_toilet_paper.svg`
    `main_v2/img/icons/cosmetics_tweezers.svg`

## Актуализация 2026-07-01

- Обновлена матрица `Типа ном. кол-ва` по подкатегориям:
  - `decor_ukhod`

## Актуализация 2026-07-03

- Исправлены backend/regression-места после переноса `cosmetics` в `2.0r`:
  - `preprocess_order_common(...)` снова распознаёт `settings.Cosmetics.CATEGORY`
  - `Category.check_subcategory(...)` снова знает подкатегории `cosmetics`
  - общая проверка `color` больше не применяется к `cosmetics`
  - для `cosmetics` возвращено чтение `subcategory` из `request.view_args`
  - сохранение больше не падает на `layers_characteristic=None`
- Исправлены ссылки и маршруты `subcategory`-based flow:
  - `clean_orders` в общей modal-логике
  - `copy_order`/открытие черновика из `orders_archive`
  - `order_table_update(...)` и preload/open flows
- Исправлены черновики и пользовательская модалка `order_book_detail`:
  - `cosmetics` добавлена в список категорий через `settings.Cosmetics.CATEGORY`
  - черновики группируются по `subcategory`
  - для открытия и очистки черновика используются route-ы с `subcategory`
- `orders_archive` приведён к логике `Markineris-2.0`:
  - копия заказа `cosmetics` открывается с `subcategory`
  - в архивном запросе восстановлен `GROUP BY` по `Cosmetics.subcategory`
  - в строке архивного заказа снова показывается подкатегория косметики
    базово:
    `мл`, `л`, `г`, `кг`
    для `МАСКА`:
    `г`, `мл`
  - `cosmetics_eye`
    использует ту же матрицу, что `decor_ukhod`
  - `cosmetics_lips`
    использует ту же матрицу, что `decor_ukhod`
  - `cosmetics_the_rest_hair`
    базово:
    `мл`, `л`, `г`, `кг`
    для `МАСКА` и `МАСКА ОТТЕНОЧНАЯ`:
    `г`, `мл`
  - `cosmetics_tooth`
    базово:
    `мл`, `л`, `г`, `кг`, `мм`, `см`, `м`
    для группы
    `АЭРОЗОЛЬ`, `ВОСК`, `ГЕЛЬ`, `ЖИДКОСТЬ ДЛЯ ИРРИГАТОРА`, `ОПОЛАСКИВАТЕЛЬ`, `ОСВЕЖАЮЩИЙ СПРЕЙ`, `ПАСТА`, `ПАСТА-ГЕЛЬ`, `ПЕНКА`, `ПОРОШОК`, `САЛФЕТКИ ДЛЯ ЧИСТКИ ЗУБОВ`, `ТАБЛЕТКА`
    оставлены только:
    `мл`, `л`, `г`, `кг`
    для группы
    `ЗУБОЧИСТКА С ЗУБНОЙ НИТЬЮ`, `НИТЬ ДЛЯ ЧИСТКИ ЗУБОВ`, `ФЛОССЕР`, `ШЁЛК ЗУБНОЙ`
    оставлены:
    `шт`, `мм`, `см`, `м`
    для группы
    `НАСАДКА СМЕННАЯ ДЛЯ ЩЁТОК`, `ЩЁТКА`, `ЩЁТКА ДЛЯ ЗУБНЫХ ПРОТЕЗОВ`, `ЩЁТКА ДЛЯ ЯЗЫКА`, `ЩЁТКА ЗУБНАЯ`, `ЩЁТКА ЗУБНАЯ ОРТОДОНТИЧЕСКАЯ`
    оставлен только:
    `шт`
  - `cosmetics_nails`
    базово:
    `мл`, `л`, `г`, `кг`, `шт`
    для инструментов и наборов:
    только `шт`
  - `cosmetics_rascheski`
    только `шт`
  - `cosmetics_mochalki`
    только `шт`
  - `cosmetics_salt_bomb`
    только:
    `мл`, `л`, `г`, `кг`
  - `cosmetics_aroma`
    использует базу `decor_ukhod`, то есть:
    `мл`, `л`, `г`, `кг`
  - `cosmetics_cleaning_products`
    использует базу `decor_ukhod`, то есть:
    `мл`, `л`, `г`, `кг`
  - `cosmetics_deodorants`
    использует базу `decor_ukhod`, то есть:
    `мл`, `л`, `г`, `кг`
  - `razor_blades_and_cassettes`
    без изменений:
    `шт.`, `пакетик`, `пакетиков`, `саше`, `пара`, `пары`
- Для `cosmetics_toilet_paper` и `cosmetics_tweezers` добавлены подписи в общий
  `SUB_CATEGORIES_DICT`:
  - это нужно для hover-title в CRM и в общих шаблонах, где название подкатегории берётся не из cosmetics registry, а из `settings.SUB_CATEGORIES_DICT`
  - без этого в tooltip показывалось просто `косметика`
- Для `cosmetics_toilet_paper` заменена CRM-иконка:
  - в `static/crm_mod/img/icons/cosmetics_toilet_paper.svg`
    сохранена исходная форма рулона из пользовательской иконки
  - убраны заливки
  - оставлен только контур под CRM-стиль
  - подключение CRM-шаблонов переведено на эту отдельную contour-иконку
  - cache-bust для CRM-иконки поднят до:
    `20260630-toilet-paper-outline`

## Правило ведения

При каждом изменении `cosmetics` обновлять этот файл:

- что добавили
- что сломано/починили
- какие договоренности появились
- какой следующий технический шаг
